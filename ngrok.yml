version: "2"
authtoken: 2TX1W9PXk4cLg8LG7NK26zDM748_37YdxpfsogTtBWiGq9do6

# AI Profile Photo Maker - Performance Optimized Configuration
tunnels:
  # Frontend tunnel - Angular UI (port 4200)
  frontend:
    addr: 4200
    proto: http
    domain: awlocaldev.ngrok.app
    inspect: false  # Disabled for performance
    compression: true
    
  # Backend API tunnel - .NET Core API (port 5035) 
  backend:
    addr: 5035
    proto: http
    domain: awlocaldev-api.ngrok.app
    inspect: false  # Disabled for performance
    compression: true

# Global configuration - optimized for performance
console_ui: false
log_level: warn  # Reduced logging for performance
log_format: json
heartbeat_interval: 2m  # Reduced frequency
heartbeat_tolerance: 10s  # Increased tolerance
inspect_db_size: -1  # Disable inspection database completely

# Let ngrok auto-select best region instead of forcing US
# region: us  # Commented out to allow auto-selection