# Minimum Images Spec

Legacy advanced training upload flow is no longer the default authenticated product UI. The OpenAI-first Photo Workspace starts from one photo at `/app/enhance`. Replicate/custom-model training remains backend-supported as an advanced/fallback capability, not via the removed product dashboard UI.
