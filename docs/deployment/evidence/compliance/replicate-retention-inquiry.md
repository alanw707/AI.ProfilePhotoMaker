# Replicate Training Retention Inquiry

Last updated: 2025-12-20  
Status: Draft email prepared; on hold (using general consent disclosure).

## Purpose
Request written confirmation of retention periods for training inputs and model artifacts so we can align our user-facing retention disclosures.

## Draft email
To: privacy@replicate.com
Subject: Request for training data & model artifact retention details (Replicate API)

Hello Replicate team,

We use Replicate for model training and image generation. We have already reviewed your predictions retention policy (API predictions removed after one hour by default), but we need clarification on training data and model artifact retention for compliance documentation.

Could you confirm:
1. Retention period for training data inputs uploaded for model training.
2. Retention period for trained model artifacts (customer fine-tuned models).
3. Whether deletion of a model via API removes related training data and artifacts, and any delays.
4. Any regional/data residency considerations that affect retention.
5. Whether any default retention can be configured or shortened for enterprise accounts.

We will use this to update our retention policy disclosures and compliance evidence.

Thanks,
[Name]
[Company]
[Account email]
