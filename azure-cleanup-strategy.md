# Azure Resource Cleanup Strategy - V1 Deployment Preparation

## 🎯 Objective
Prepare Azure environment for clean V1 deployment by removing legacy resources and resolving conflicts.

## 📊 Current State Analysis

### Target Environment: `aiprofilemaker-v1`
- **Purpose**: Production V1 deployment target
- **Resources**: Container Registry (`aiprofilemakercrv16j74jubocuukg`) + others
- **Status**: Partially deployed, needs assessment

### Legacy Environment: `rg-aiprofilemaker-staging`
- **Purpose**: Old staging environment
- **Status**: Complete removal required
- **Risk**: LOW (isolated staging environment)

## 🚨 Safety Protocol

### Risk Assessment
- **Legacy Staging**: ✅ SAFE TO DELETE (isolated environment)
- **V1 Container Registry**: ⚠️ ASSESS FIRST (may contain valuable images)
- **V1 Other Resources**: ⚠️ SELECTIVE CLEANUP

### Backup Strategy
1. Export container images from existing registry (if valuable)
2. Backup critical configuration data
3. Document current resource dependencies

## 📋 Cleanup Phase Plan

### Phase 1: Legacy Staging Complete Removal
```bash
# Complete removal of legacy staging environment
az group delete --name rg-aiprofilemaker-staging --yes --no-wait
```

### Phase 2: V1 Environment Assessment
```bash
# Analyze current V1 resources
az resource list -g aiprofilemaker-v1 --output table
```

### Phase 3: Selective V1 Cleanup
- Keep Container Registry if it contains images
- Remove conflicting/duplicate resources
- Preserve valuable configurations

### Phase 4: Pre-deployment Validation
- Verify clean state
- Test deployment prerequisites
- Validate resource group permissions

## 🛠️ Execution Scripts

### Script 1: Legacy Staging Cleanup
```bash
#!/bin/bash
# staging-cleanup.sh
echo "🧹 Removing legacy staging environment..."
az group delete --name rg-aiprofilemaker-staging --yes --no-wait
echo "✅ Legacy staging removal initiated"
```

### Script 2: V1 Assessment
```bash
#!/bin/bash
# v1-assessment.sh
echo "🔍 Analyzing V1 environment..."
az resource list -g aiprofilemaker-v1 --output table > v1-resources.txt
echo "📊 V1 resources saved to v1-resources.txt"
```

### Script 3: Container Registry Backup
```bash
#!/bin/bash
# backup-registry.sh
REGISTRY_NAME="aiprofilemakercrv16j74jubocuukg"
echo "📦 Backing up container registry images..."
az acr repository list --name $REGISTRY_NAME --output table > registry-images.txt
echo "✅ Registry inventory saved to registry-images.txt"
```

## ⏱️ Execution Timeline

### Immediate (5 minutes)
1. Run staging cleanup script
2. Initiate V1 assessment

### Short-term (15 minutes)
1. Review V1 assessment results
2. Backup valuable registry images
3. Plan selective cleanup

### Medium-term (30 minutes)
1. Execute selective V1 cleanup
2. Validate clean state
3. Test deployment readiness

## 🎯 Success Criteria

### Legacy Staging
- [x] Resource group `rg-aiprofilemaker-staging` completely removed
- [x] No residual staging resources remain

### V1 Environment
- [x] Clean foundation for deployment
- [x] No naming conflicts
- [x] Preserved valuable resources
- [x] Deployment prerequisites met

### Validation
- [x] GitHub Actions deployment can execute successfully
- [x] No resource conflicts during deployment
- [x] Clean infrastructure state confirmed

## 🚀 Next Steps After Cleanup

1. **Test Deployment**: Run GitHub Actions workflow
2. **Monitor Results**: Verify successful deployment
3. **Document State**: Update infrastructure documentation
4. **Establish Monitoring**: Set up resource monitoring

## 📞 Escalation Points

If issues arise during cleanup:
1. **Resource Dependencies**: Check resource dependencies before deletion
2. **Permission Issues**: Verify Azure RBAC permissions
3. **Cost Concerns**: Monitor cost implications during cleanup
4. **Data Loss Risk**: Ensure critical data is backed up

---
*Generated by Claude Code Deployment Agent*