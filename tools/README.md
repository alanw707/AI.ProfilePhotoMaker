# tools

This directory is reserved for developer automation and CLI utilities. Keep helper scripts and prototypes that support local workflows here. If no tooling is required, the folder can be removed safely.
